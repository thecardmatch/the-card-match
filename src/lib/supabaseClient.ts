// Supabase has been removed. This file is kept as a stub so old import
// paths resolve without errors while the migration completes.
export const supabase = null;
export const isSupabaseReady = false;
